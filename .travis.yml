sudo: false
language: c
 
addons:
  apt:
    packages:
    - pandoc
 
script: curl -sO https://raw.githubusercontent.com/BigWigsMods/packager/master/release.sh

branches:
  only:
  - /^v\d+\.\d+(\.\d+)?(-\S*)?$/
